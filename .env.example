# PostgreSQL connection string (Neon / Supabase / local)
DATABASE_URL="postgresql://user:password@host:5432/trackpile?sslmode=require"

# Auth.js — generate with: npx auth secret
AUTH_SECRET="your-generated-secret"

# Google OAuth credentials (Google Cloud Console → Credentials → OAuth client ID)
# Authorized redirect URI: http://localhost:3000/api/auth/callback/google
AUTH_GOOGLE_ID="your-google-client-id"
AUTH_GOOGLE_SECRET="your-google-client-secret"

# For production, also set:
# AUTH_URL="https://your-domain.com"

# Public base URL used to build AbacatePay return/completion redirect URLs.
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# AbacatePay (checkout/billing) — https://docs.abacatepay.com
# Use a sandbox/devMode key while developing.
ABACATEPAY_API_KEY="your-abacatepay-api-key"
ABACATEPAY_BASE_URL="https://api.abacatepay.com/v2"
# Secret configured when creating the webhook (POST /webhooks/create) — also
# appended to the endpoint URL as ?webhookSecret=<value>.
ABACATEPAY_WEBHOOK_SECRET="your-webhook-secret"
